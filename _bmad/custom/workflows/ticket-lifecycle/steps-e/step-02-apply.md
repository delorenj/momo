# Edit Step 2: Apply Client-Policy Change

## Actions

1. Update only allowed Momo client-policy/display configuration.
2. Preserve fail-closed behavior when Lifecycle is unavailable.
3. Preserve required snapshot/command version and capability fields.
4. Regenerate every tracked duplicate workflow copy byte-for-byte.
5. Run validation mode and the contradiction scan.

If the request changes lifecycle semantics or schemas, stop and hand it to the
Lifecycle or Bloodbank owner rather than applying it here.
